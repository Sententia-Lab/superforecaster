#!/bin/sh
# Test gate: nothing leaves this machine with a red suite.
# Enable once per clone:  git config core.hooksPath scripts/hooks
set -e
cd "$(git rev-parse --show-toplevel)/backend"
echo "pre-push: running backend tests..."
uv run pytest -q
