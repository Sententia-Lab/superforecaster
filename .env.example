# Backend settings live in one place:
#
#   cp backend/.env.example backend/.env
#
# Docker Compose reads backend/.env directly. The frontend has no env file — it is
# static files served by the API (dev-time Vite proxies to the API port instead).
# See backend/config.py for the full settings surface.
