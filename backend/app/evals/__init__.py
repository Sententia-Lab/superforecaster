"""Evaluation harnesses.

`components.py` scores individual agents against per-agent golden data. The
end-to-end backtest over resolved questions is specified in
`spec/change_specs/spec4.md` and is not built yet — it needs a corpus of questions
asked after the earliest available model's training cutoff.
"""
